function solution(n) {
  let answer = 0;
  const arr = Array.from({ length: n }, () =>
    Array.from({ length: n }, () => 0)
  );
  function DFS(L){
    if(L===n*n){
      
    }
  }
  return answer;
}

console.log(solution(3));
